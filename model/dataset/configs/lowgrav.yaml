# CoinRun — low gravity (floaty jumps, slower falls).
# Counterfactual on the physics: lower downward acceleration changes jump arcs and
# fall speed while layout/actions stay fixed under a shared seed.
name: lowgrav
game: coinrun
physics:
  gravity: 0.1   # vanilla 0.2
